using UnityEngine;

public class HadAbility : MonoBehaviour
{
    public string Name;
    public string id;
    public CONST.ABILITY.Category category;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {

    }
}
